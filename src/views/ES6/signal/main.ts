// import { observable } from './observable';

const btn = document.querySelector('button');

console.log(btn);

// const count = observable(0);
//
// count.subscribe((count: number) => {
//   btn.innerText = count + '';
// });
//
// const unsubscribe = count.subscribe((count: number) => {
//   console.log(count);
// });
//
//
// btn.addEventListener('click', () => {
//   count.increment();
// });
